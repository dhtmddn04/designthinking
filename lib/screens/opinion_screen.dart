import 'package:flutter/material.dart';

class OpinionScreen extends StatelessWidget {
  const OpinionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('실시간 의견 화면', style: TextStyle(fontSize: 24)),
    );
  }
}
